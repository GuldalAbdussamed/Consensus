# Engelsiz TV pipeline - runtime deps

# Async HTTP (VLM + TTS)
openai>=1.30
httpx>=0.27

# Görüntü işleme
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0

# VAD
silero-vad>=5.0
torch>=2.0
soundfile>=0.12

# Ses çıkışı (sounddevice opsiyonel - yoksa WAV fallback)
sounddevice>=0.4
